// Kept as a deprecated stub — replaced by AppBottomBar in the chrome refactor.
// Plans tab removed per website IA (packages not in nav, cap-gated only).
export function PublicFooterNav() {
  return null;
}
